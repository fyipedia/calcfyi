# Smithery configuration for calcfyi
# https://smithery.ai/docs/build/project-config
startCommand:
  type: stdio
  configSchema:
    type: object
    required: []
    properties: {}
  commandFunction: |-
    config => ({ command: 'uvx', args: ['--from', 'calcfyi[mcp]', 'python', '-m', 'calcfyi.mcp_server'] })
